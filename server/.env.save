# Server config
PORT=5000
NODE_ENV=development

# Client URL for CORS — set to your Netlify URL in production
# Supports comma-separated values for multiple origins
# Example: CLIENT_URL=https://your-app.netlify.app,https://custom-domain.com
CLIENT_URL=http://3.110.148.69

# SMTP config for email OTP verification
# For Gmail: enable "App Passwords" in Google account settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ashok.j234
SMTP_PASS=your-app-password

# If SMTP is not configured, OTP codes will be logged to the server console (dev mode)
